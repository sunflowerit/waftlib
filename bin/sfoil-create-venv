#!/bin/sh

if test "$#" -ne 2; then
    echo "Usage: $0 python-executable venv-dir"
    echo "Eg: $0 python3 .venv"
    exit 1
fi

if ! test -d "$2"; then
    virtualenv --python "$1" "$2"
else
    echo "Warning: virtualenv already built, ignoring Python version"
fi
